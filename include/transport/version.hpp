#pragma once

// -----------------------------------------------------------------------------
// version.hpp — 库版本号
// 提供 transport::LibraryVersion()，返回库的语义化版本字符串。
// -----------------------------------------------------------------------------

#include <string>

namespace transport {

std::string LibraryVersion();

}  // namespace transport
